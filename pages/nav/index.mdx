import React from 'react'
import { Aside } from '../../components/aside'
import { NavData } from '../../data/data'
import Content from '../../components/content'
import { Top } from '../../components/Top'

{/* 导航 Nav */}
<div id='content-start' className={'w-full'}>
  <div className={'h-96 w-3/4 bg-gradient-to-r -z-10 from-blue-500 to-red-800 absolute right-0 -top-12 opacity-10 dark:opacity-20 !blur-3xl rounded-full'}></div>
  <div className={'flex flex-col w-full justify-start h-24 items-center my-3 mt-12'}>
    <h1 className={'text-5xl font-bold underline decoration-orange-400 underline-offset-4'}>
      Navigation
    </h1>
    <div className={'mt-6'}>
      站点收藏及常用导航
    </div>
  </div>
  <div className='w-full relative py-5 flex border-t-2 border-gray-300 dark:border-gray-700 border-dashed '>
    <div className={'top-0 w-[30rem] lg:flex'}>
      <Aside data={NavData} />
    </div>
    <div className={''}>
      <Content nav={NavData} />
    </div>
  </div>
  <Top />
</div>
